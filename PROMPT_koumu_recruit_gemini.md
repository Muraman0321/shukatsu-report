# 官公庁・政府系126機関 採用ページ判定プロンプト（Geminiへの引き渡し）

`shukatsu-data.com`（就活サイト）の官公庁・政府系ページに「採用ホームページ」リンクを追加するための、
採用ページ判定バッチ。`fetch_koumu_links.py`が各機関の公式サイトのトップページから機械的に集めた
候補リンクの中から、実際の新卒採用ページを1つ選んでもらう。**このファイルの
「Geminiへ渡すプロンプト本文」セクションをそのままGeminiに渡す。**

---

## 0. 使い方（人間向けメモ）

- 入力：`gemini_batches/koumu_recruit_batch_NN.txt`（`tools/build_koumu_recruit_batches.py`が生成、
  1バッチ最大35機関）
- 各バッチについて、下記「プロンプト本文」の後ろに該当バッチのテキストをそのまま貼り付けてGeminiに渡す
- 返ってきたJSON配列を `gemini_output/koumu_recruit_batch_NN.json` として保存する
  （コードブロックの```json ... ```で囲まれたままでもよい）
- 全バッチぶん揃ったら `python tools/split_koumu_recruit_output.py` を実行して
  `data/koumu/{slug}.json` に反映する

---

## 1. Geminiへ渡すプロンプト本文（ここからそのまま渡す）

あなたは就活情報サイト「shukatsu-data.com」の官公庁・政府系ページ制作アシスタントです。

このサイトは省庁・独立行政法人・政府系金融機関の新卒採用情報を、各機関の公式発表だけを
出典にまとめています。あなたの仕事は、クローラーが各機関の公式サイトのトップページから
機械的に集めた**候補リンクの一覧**を読み、その中から実際の新卒採用ページを1つ選ぶことです。

### 絶対に守ること

1. **渡された候補リンクの中からしか選ばない**：候補にない新しいURLを作らない・推測しない・
   末尾を変えない。候補の`url`の文字列をそのまま転記する。
2. **あなた自身の知識を使わない**：この機関の採用ページについて知っていることがあっても
   使わない。判断材料は渡された候補リンク（textとurl）だけ。ウェブ検索・外部知識の参照は禁止。
3. **該当する候補が無ければ`recruit: null`にする**：候補が「候補なし」と書かれている機関、
   または候補はあるが新卒採用ページと呼べるものが無い機関（例：職員採用ではなく物品調達の
   入札情報、OB/OG向けの同窓会ページ等が紛れている場合）は、無理に選ばず`null`にする。
4. **1機関につき1件だけ選ぶ**：複数の候補が採用関連に見える場合は、最も「新卒・総合職・
   本体の採用ページ」に近いものを1つ選ぶ（個別の専門職試験ページより、機関全体の採用トップ
   ページを優先する）。

### 入力データの形式

各機関は以下の形（バッチテキストの区切り）：

```
======================================================================
[slug] 機関名（kind）
- text="候補リンクの表示テキスト"  url=https://example.go.jp/saiyo/
- text="別の候補"  url=https://example.go.jp/saiyo/entry.html
```

候補が無い機関は `(候補なし)` とだけ書かれています。

### 出力スキーマ

バッチ内の**全機関ぶん**（`(候補なし)`の機関も含む）について、以下の形のJSONオブジェクトを
1つ作る。**出力は配列**（入力機関数と出力配列の長さが必ず一致するようにする）：

```json
{
  "slug": "aist",
  "recruit": "https://www.aist.go.jp/aist_j/humanres/03jimu/general1.html",
  "recruit_text": "総合職（事務系）採用ページ"
}
```

- `slug`：入力の`[slug]`をそのまま転記
- `recruit`：選んだ候補の`url`をそのまま転記。該当なしなら`null`
- `recruit_text`：選んだ候補の`text`をそのまま転記（人間が読んでボタンに表示する文言として
  そのまま使えるよう、要約・言い換えをしない）。`recruit`が`null`なら`null`

### 出力形式

説明文やMarkdownの前置きを付けず、**JSON配列のみ**を出力してください（コードブロックで
囲んでよい）。バッチ内の全機関ぶんを含める（`null`の機関を除外しない）。

---

## 2. Step3（Claude・後続セッション）への申し送り事項

- Geminiの出力を`data/koumu/{slug}.json`に反映する前に、`tools/split_koumu_recruit_output.py`が
  `recruit`のURLが実際にそのslugへ渡した候補リンクのいずれかと一致するか機械的に検証する
  （一致しないものは捏造・書き換えの疑いがあるとしてスキップし、警告を出す）
- 反映後、サンプル数件を目視でも確認する（候補の中から選んだだけとはいえ、機関名と
  URLの組み合わせが不自然でないか）

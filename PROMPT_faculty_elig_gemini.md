# 学部適性の構造化プロンプト（Geminiへの引き渡し）

`shukatsu-data.com`（就活サイト）の「就活の軸診断」に学部フィルタを足すための、学部適性
構造化バッチ。`data/saiyo/{code}.json` の `target_faculty`（採用ページに実際に書かれていた
対象学部の自由記述）を読み、文系／理系（工学・情報系）／理系（生命科学系）の3区分それぞれに
ついて受験できるかを判定してもらう。**このファイルの「Geminiへ渡すプロンプト本文」セクション
をそのままGeminiに渡す。**

---

## 0. 使い方（人間向けメモ）

- 入力：`gemini_batches/faculty_elig_batch_NN.txt`（`tools/build_faculty_elig_batches.py`が生成、
  1バッチ最大40社）
- 各バッチについて、下記「プロンプト本文」の後ろに該当バッチのテキストをそのまま貼り付けてGeminiに渡す
- 返ってきたJSON配列を `gemini_output/faculty_elig_batch_NN.json` として保存する
  （コードブロックの```json ... ```で囲まれたままでもよい）
- 全バッチぶん揃ったら `python tools/split_faculty_elig_output.py` を実行して
  `data/saiyo/{code}.json` に反映する（先に `--dry-run` で検証結果だけ確認してもよい）

---

## 1. Geminiへ渡すプロンプト本文（ここからそのまま渡す）

あなたは就活情報サイト「shukatsu-data.com」の「就活の軸診断」制作アシスタントです。

このサイトは各社の採用ページに実際に書かれていた対象学部の文言（`target_faculty`）だけを
出典にしています。あなたの仕事は、その文言を読み、以下の3区分それぞれについて
「その学部の学生がこの会社を受験できるか」を判定することです。

### 3つの区分

- `bunkei`：文系（法・経済・商・経営・文学部など）
- `rikei_kougaku`：理系（工学・情報系。工学・情報・機械・電気・電子など）
- `rikei_seimei`：理系（生命科学系。薬学・農学・理学・生命科学・化学など）

### 絶対に守ること

1. **渡された `target_faculty` の文言だけから判定する**：この会社について知っている一般的な
   知識を使わない。ウェブ検索・外部知識の参照は禁止。
2. **各区分の値は次の3つのどれかにする**：
   - `true`：文言から、その区分の学生が受験できると明確に読み取れる
     （例：「全学部全学科」「文理不問」「事務系：学部・学科不問」であれば bunkei も両方の
     rikei も true）
   - `false`：文言から、その区分の学生は対象外だと明確に読み取れる
     （例：「技術系：理系のみ」で技術系しか採用していない会社なら bunkei は false。
     「事務系：文理不問」の記載が別途あれば bunkei は true になる点に注意）
   - `null`：文言だけでは判断できない（職種ごとに条件が違って区分と対応づけられない、
     文言が曖昧、機械・電気系など理系の一部の学科名しか列挙されておらず理系の他の学科が
     対象かどうか読み取れない、等）
3. **迷ったら `null` にする**：わずかな手がかりから拡大解釈で `true`/`false` を断定しない。
   `null` は「除外しない」扱いになるので、安全側に倒すなら `null` を選ぶ。
4. **理系の学科名の割り振り**：機械・電気・電子・情報・土木・建築・材料・化学工学などは
   `rikei_kougaku`、薬学・農学・理学・生命科学・化学（工学よりの化学工学ではなく理学寄りの
   化学）などは `rikei_seimei`。両方にまたがる列挙（例：「理系全般」「理工系」とだけ書かれて
   学科名の列挙が無い）は両方 `true` にしてよい。

### 入力データの形式

各社は以下の形（バッチテキストの区切り）：

```
======================================================================
[E00021] 三菱マテリアル株式会社
target_faculty: 技術系：理系のみ。事務系：文理不問（学部・学科不問）。
```

### 出力スキーマ

バッチ内の**全社ぶん**について、以下の形のJSONオブジェクトを1つ作る。**出力は配列**
（入力社数と出力配列の長さが必ず一致するようにする）：

```json
{
  "code": "E00021",
  "bunkei": true,
  "rikei_kougaku": true,
  "rikei_seimei": null
}
```

- `code`：入力の`[code]`をそのまま転記
- `bunkei` / `rikei_kougaku` / `rikei_seimei`：それぞれ `true` / `false` / `null` のいずれか

### 出力形式

説明文やMarkdownの前置きを付けず、**JSON配列のみ**を出力してください（コードブロックで
囲んでよい）。バッチ内の全社ぶんを含める（`null`を含む会社を除外しない）。

---

## 2. Step3（Claude・後続セッション）への申し送り事項

- Geminiの出力を`data/saiyo/{code}.json`に反映する前に、`tools/split_faculty_elig_output.py`が
  `bunkei`/`rikei_kougaku`/`rikei_seimei`それぞれの値が`true`/`false`/`null`のどれかであることを
  機械的に検証してから書き込む（それ以外の値が来たら捏造・出力崩れの疑いがあるとしてスキップし、
  警告を出す）
- 反映後、`false`が付いた会社を数件サンプルで元の`target_faculty`と突き合わせ、拡大解釈で
  除外していないか目視確認する（就活の軸診断は「明確に対象外の会社だけ除外」が原則で、
  誤って`false`にすると受けられる会社を隠してしまう）
- `python generate.py`を再実行すれば、反映済みの`target_faculty_eligibility`が自動的に
  診断結果の除外に反映される（コード側の変更は不要）
